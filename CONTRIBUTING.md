# 贡献指南

## 提交问题

请说明 Windows 版本、Python 版本、运行方式、复现步骤、预期行为和实际行为。截图需要先遮盖门店、人员、路径和订单等敏感信息。

匹配准确率问题请描述可泛化的特征，例如“同款不同色时小胸标被忽略”，不要只给某个固定款号要求写死答案。

## 提交代码

1. 从 `main` 创建功能分支。
2. 只修改当前任务需要的文件。
3. 运行 `python -m py_compile app.py engine.py verify_imports.py verify_export.py verify_v8.py`。
4. 有本地测试数据时运行 V8 三个 `verify_*.py` 脚本。
5. 在 Pull Request 中说明变更、验证结果和界面影响。

## 禁止提交

- 原始图库、门店实拍图或生成结果；
- 模型权重、索引、缓存和本地反馈；
- `build/`、`dist/` 和虚拟环境；
- Token、密码、网盘入口、私人路径或账号资料；
- 只对某个固定图库或款号有效的硬编码答案。

贡献代码默认受仓库当前许可证状态约束；在项目明确许可证前，请勿假设获得再分发授权。
